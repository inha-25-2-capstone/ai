# 자동 라벨링 스크립트용 의존성
# scripts/ 폴더에서 사용하는 LLM API 패키지

# 기본 의존성 포함
-r requirements.txt

# LLM APIs (자동 라벨링용)
openai>=1.3.0
google-generativeai>=0.3.0

# 데이터 수집용 (필요시)
beautifulsoup4>=4.12.0
selenium>=4.15.0
