# 자동 라벨링 스크립트용 의존성
# scripts/ 폴더에서 사용하는 LLM API 패키지

# 기본 의존성 포함
-r requirements.txt

# LLM APIs (자동 라벨링용)
openai>=1.30.0                   # ChatGPT API + Batches API
anthropic>=0.18.0                # Claude API + Batches API

# 데이터 수집용 (필요시)
beautifulsoup4>=4.12.0
selenium>=4.15.0
